<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kingwag.tinyworld">

    <!-- 允许联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 获取GSM（2g）、WCDMA（联通3g）等网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.view.activity.MineFragment_RegisterActivity" />
        <activity android:name=".view.view.activity.MineFragment_RegisterAgreement" />
        <activity android:name=".view.view.activity.MyFragment_LoginActivity" />
        <activity android:name=".view.view.activity.MineFragment_RegisterActivity" />
        <activity android:name=".view.view.activity.MineFragment_RegisterAgreement" />
        <activity android:name=".view.view.activity.MineFragment_OpinionActivity"></activity>
    </application>

</manifest>